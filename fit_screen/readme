完美实现适配所有屏幕分辨率

环境:
    cocos3.4
    cocos studio 2.1 + 布局系统

cocos studio 设计说明:

    1. 顶层节点为scene类型
    2. panel
        !!! 宽度和高度都用 100 %，而不要写死为设计分辨率
        裁切内容 选择为true，其实不勾也没啥

    3. 内部元素设计xy坐标时，想要使用图钉，就不能用百分比，只能用像素
